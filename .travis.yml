language: go

go:
  - stable

before_install:
  - go get -u golang.org/x/vgo
  - go get -u github.com/alecthomas/gometalinter
  - gometalinter --install

install:
  - vgo vendor

script:
  - make lint
  - make test
  - make
